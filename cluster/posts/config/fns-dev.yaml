log:
  level: debug
  formatter: json
server:
  port: 8800
cluster:
  devMode: true
  nodesProxyAddress: proxyHost
  kind: members
  client:
    maxIdleConnSeconds: 60
    requestTimeoutSeconds: 10
  options:
    members:
    - ip:port
